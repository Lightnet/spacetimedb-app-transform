# Information:
  Note the build.ts script to merge files into single file due to schedule tables.

Create the table files must be a part to able to assign schema tables.

```
bun run build.ts
```
 - note it has be in spacetimedb folder for this current folder to look for src folder to build file.